.DS_Store
.hugo_build.lock
/public
/resources
# Playwright MCP scratch output
.playwright-mcp/

# Personal, machine-specific Claude Code overrides. Interactively-approved
# permissions land here verbatim, tokens and absolute home paths included --
# the shared config belongs in .claude/settings.json, which is committed.
.claude/settings.local.json
